[project]
name = "vk-handshakes-app"
version = "0.1.0"
description = "Local desktop app for VK handshakes"
requires-python = ">=3.11"
dependencies = [
  "PySide6>=6.7",
  "requests>=2.32",
  "pydantic>=2.7",
  "keyring>=25.0",
  "loguru>=0.7",
]

[tool.black]
line-length = 100
